<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.js.androlearn">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Androlearn">
        <activity android:name=".activities.ConnectITunesActivity"></activity>
        <activity android:name=".activities.PreferencesActivity" />
        <activity android:name=".activities.NotificationActivity" />
        <activity android:name=".activities.SharedPrefsActivity" />
        <activity android:name=".activities.ImageDownloadActivity" />
        <activity
            android:name=".activities.ThirdTabActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".activities.SecondTabActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".activities.FirstTabActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".activities.TabHostActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".activities.AccessContactActivity" />
        <activity android:name=".activities.ViewFlipperAnimatorActivity" />
        <activity android:name=".activities.ImageSwitcherActivity" />
        <activity android:name=".activities.GallaryScrollViewActivity" />
        <activity android:name=".activities.GridViewActivity" />
        <activity android:name=".activities.CustomizeListView" />
        <activity android:name=".activities.SecondActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Preferences1Activity" />
    </application>

</manifest>